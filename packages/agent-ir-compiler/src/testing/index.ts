/**
 * @orbit/agent-ir-compiler/testing
 *
 * Fixtures for workflows that are compiled in tests, behind a subpath so
 * production code cannot reach them — the same arrangement as
 * `@orbit/sop-graph/testing` and `@orbit/execution-mapping/testing`.
 */
export * from './library-demo';
export * from './mortgage-demo';
