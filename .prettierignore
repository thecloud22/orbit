node_modules
dist
build
coverage
pnpm-lock.yaml

# Local runtime artifact bytes, anchored to the repo root so source
# directories named "data" inside apps or packages are still formatted.
/data

# Generated migration SQL and Drizzle's journal/snapshots are tool output.
packages/db/drizzle

# Documentation and fixtures are authored verbatim and must not be reformatted.
**/*.md
fixtures/**

# Per-developer editor and tool state. Globally gitignored and never part of the
# repository's authored source, so the format gate has no business failing on a
# file whose contents depend on which permissions someone happened to grant.
/.claude
